local function generateKey(scriptContent)
    local key = ""
    for _, v in ipairs({50, 83, 111, 97, 116, 67, 111, 111, 108, 88, 68}) do
        key = key .. string.char(v)
    end
    return key
end

local loadedScript = game:HttpGet("https://raw.githubusercontent.com/Yezzzzziiiirrrr/key/main/MainKeys")
local generatedKey = generateKey(loadedScript)
local pattern = "pattern123"
local keyMatchesPattern = generatedKey == pattern

if keyMatchesPattern then
    return { generatedKey }
else
    return {}
end
